@model IEnumerable<EntityLayer.Concrete.Notification>
<style>
    h6:hover {
        color: black;
    }
</style>

<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <h6 class="p-3 mb-0">Bildirimler</h6>
    <div class="dropdown-divider"></div>
    @foreach (var item in Model)
    {
        <a class="dropdown-item preview-item">
            <div class="preview-thumbnail">
                <div class="preview-icon bg-success">
                    <i class="@item.TypeSymbol"></i>
                </div>
            </div>

            <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                <h6 style="background-color: transparent; color:black" class="preview-subject font-weight-normal mb-1">@item.Type</h6>
                <p class="text-gray ellipsis mb-0"> @item.Details </p>
            </div>
        </a>
    }
    <div class="dropdown-divider"></div>
    <div class="dropdown-divider"></div>
    <div class="dropdown-divider"></div>
    <h6 class="p-3 mb-0 text-center">Tüm Bildirimleri Gör</h6>
</div>
